<template>
  <section class="app-main">
    <transition name="fade-transform" mode="out-in">
      <router-view :key="key" />
    </transition>
    <footer v-if="showFooter"> <!-- 显示底部栏 -->
      <!-- 添加底部栏的 HTML 和 CSS 代码 -->
    </footer>
  </section>
</template>

<script>
export default {
  name: 'AppMain',
  computed: {
    key() {
      return this.$route.path
    },
    showFooter() {
      const { meta = {}} = this.$route
      return Boolean(meta.isFooter) // 如果路由的 meta.isFooter 属性为 true，则显示底部栏
    }
  }
}
</script>

<style scoped>
.app-main {
  /*50 = navbar  */
  min-height: calc(100vh - 50px);
  width: 100%;
  position: relative;
  overflow: hidden;
}
.fixed-header+.app-main {
  padding-top: 50px;
}
</style>

<style lang="scss">
// fix css style bug in open el-dialog
.el-popup-parent--hidden {
  .fixed-header {
    padding-right: 15px;
  }
}
</style>
