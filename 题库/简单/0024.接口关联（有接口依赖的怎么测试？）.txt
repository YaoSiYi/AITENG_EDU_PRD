接口关联（有接口依赖的怎么测试？）
  后一个请求需要用到前一个请求的响应数据，在前一个请求后面添加一个后置处理器，比如正则表达式提取器，或者json提取器，把需要的内容提取出来保存到一个变量中，然后在后一个请求中引用这个变量就可以了
  postman关联（面试版）：
      可以把前一个请求的响应数据，在tests标签里保存为一个环境变量，然后在后续的请求中，通过两对大括号，来引用这个变量就可以了
